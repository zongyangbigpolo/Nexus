- vars:
    jira: ENG-9900
  assert:
    - type: llm-rubric
      value: |
        Evaluate whether the agent correctly handled a project initialization request for jira=ENG-9900.

        Score 1.0 if ALL of the following are true:
        - Identifies this as a full project init task for ENG-9900        
      threshold: 1
